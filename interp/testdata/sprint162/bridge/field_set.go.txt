package main

import (
	"bytes"
	"os/exec"
)

func main() {
	cmd := exec.Command("/usr/bin/env")
	cmd.Dir = "."
	var out bytes.Buffer
	cmd.Stdout = &out
	cmd.Env = []string{"S162_FIELD_SET=ok"}
	if err := cmd.Run(); err != nil {
		panic(err)
	}
	println(out.String())
}
