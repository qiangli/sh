---
id: 3cccf8001037
kind: task
title: 'Sprint 162.2: multi-package execution — the 26 cmd/compile/internal/* package roots (DESIGN FIRST: a number before code)'
seq: 92
status: done
priority: p0
created: 2026-09-13T00:36:40.636812Z
sprint: 162
closed: 2026-09-13T10:32:32.92383Z
---

0/26 in every run since Sprint 150. Interpreted: 'bashy: --go-package, --go-import-base and --go-import-path require --check or --go-list; interpreted execution of an explicit package set is not supported'. Compiled: 'could not import cmd/compile/internal/amd64 (use of internal package cmd/compile/internal/amd64 not allowed)' — the transpiled module is outside the std tree so Go's internal-import rule refuses. Both are one product gap the Sprint 150 plan ledgered: executing an upstream-prepared PACKAGE (go test of an original test body with its dependency closure) through Bash++. Step 1 (design, 1 day, no product code): a design note in sh/docs (or dhnt docs) with (a) the mechanism for interpreted execution of an explicit package map (the runtime import bridge builds imports natively today — what would make an interpreted mapped package honest), (b) the compiled path for internal packages (an overlay/-modfile placing the generated module INSIDE the SDK source tree copy, or a -trimpath'd GOROOT overlay — whichever keeps 'no native tested-source fallback'), (c) a MEASURED spike on ONE package (cmd/compile/internal/abt is the smallest) on sprint162-leaf with its number. Step 2 only after the manager records the decision on the card. Exit: the 26 roots execute their original non-zero test sets through Bash++ in the required modes, or a recorded design decision says why not and what it would cost. RULES: fixes are general Go mechanisms in sh (gosource / interp / lower), each from an OUTSIDE-CORPUS reproducer under the package testdata with the negative set; never keyed to a fixture path or expected string; never a permissive comparison; never a timeout raise. Verify: go test -count=1 on the affected packages, go test -short ./..., git diff --check (the 8 GoSource* interp tests fail on darwin before and after every change — judge by NEW failures only; never in a weave --verify), then the story leaf on sprint162-leaf (one coordinator; BASHPP_CORPUS_ROOTS=<manifest> tools/upstream-harness/corpus-gate.sh in a fresh /srv/sprint162/leaf-<name>; never the certification host, never the dev box) plus nearby native-PASS canaries. A row whose real first cause belongs to another owner moves by manifest commit, never in place. Commit trailers Sprint: #162 / Story: S162.<n> / Story-ID: <id> as the last paragraph.
